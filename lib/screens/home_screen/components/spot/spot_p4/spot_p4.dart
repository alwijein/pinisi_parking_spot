import 'package:flutter/material.dart';

class SpotP4 extends StatelessWidget {
  const SpotP4({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      mainAxisAlignment: MainAxisAlignment.spaceEvenly,
      children: [],
    );
  }
}
